import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NFT Marketplace - Discover, Create & Trade Unique NFTs",
  description: "Join the future of digital art and collectibles. Buy, sell, bid, and create unique NFTs in our decentralized marketplace.",
  keywords: ["NFT", "marketplace", "digital art", "cryptocurrency", "blockchain", "collectibles"],
  authors: [{ name: "NFT Marketplace Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
